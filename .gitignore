# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local tooling config. Holds machine-specific absolute paths and personal
# tool permissions — nothing another contributor could use, and not something
# that belongs in a public repository.
.claude/

# Secrets. Nothing in this project needs a key today (it is a fully static
# site), but these are listed so that a future integration cannot leak one by
# accident.
.env
.env.*
!.env.example
*.pem
*.key

# Source media for the Raha walkthrough. scripts/build-media.mjs reads this
# from the repo root and writes the deployable copy to public/media/raha/, so
# committing it too would put the same 13 MB in the repository twice.
/Raha - Demo.mp4

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
